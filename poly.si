foo := proc(%T: type, %U: type, %N: u32, %P: module) {
}

M := module {
    Vec3 := struct {
        x: u32, y: u32, z: u32,
    }
}

[[ program_entry ]]
main := proc() {
    foo(*M.Vec3, f32, cast(u32, 64), M);
}
