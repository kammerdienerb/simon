[[ program_entry ]]
main :: proc() {
    defer { exit(0); }
    loop i := 1; i <= 40; i += 1 {
        printf("{}", i % 10);
    }
    printf("\n");
    my_width := -20;
    printf("{=*}|\n", my_width, "hello, world!");
}
